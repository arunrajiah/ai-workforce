# HTTPS overlay config (for caddy-compose.yml in this folder).
# Veska's own settings (LLM provider, MAGIC_LINK_SECRET, DB) live in the veska
# repo's .env — see https://github.com/arunrajiah/veska/blob/main/.env.example
DOMAIN=app.example.com
ACME_EMAIL=you@example.com
