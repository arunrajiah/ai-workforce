# HTTPS overlay config (for caddy-compose.yml in this folder).
# Dify's own settings (LLM provider keys, secrets, DB) live in Dify's
# docker/.env — see https://github.com/langgenius/dify/blob/main/docker/.env.example
DOMAIN=marketing.example.com
ACME_EMAIL=you@example.com
