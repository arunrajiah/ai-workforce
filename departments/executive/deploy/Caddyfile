# Automatic HTTPS via Let's Encrypt. Caddy reads DOMAIN and ACME_EMAIL from the
# environment (set in .env). Khoj's websocket/streaming responses pass through
# automatically.

{
	email {$ACME_EMAIL}
}

{$DOMAIN} {
	reverse_proxy server:42110
}
